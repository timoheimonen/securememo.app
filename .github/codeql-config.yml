name: "CodeQL config"

paths:
  - src

paths-ignore:
  - node_modules
  - public
  - screenshots
  - db
  - '**/dist'
  - '**/build'

queries:
  - uses: security-extended
  - uses: security-and-quality
